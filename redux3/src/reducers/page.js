// import { GET_PHOTO_REQUEST, GET_PHOTO_SUCCESS } from '../actions/type'

const updatePage = state => {
  if (!state) return { year: '2018', photos: [] }

  return state
}

export default updatePage
